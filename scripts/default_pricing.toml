# Local devnet pricing configuration
#
# Subscription pricing: flat rate per billing interval.
# The pricing engine returns subscription_rate as totalCost when
# the request's pricing_model = SUBSCRIPTION (1).

[default]
pricing_model = "subscription"
subscription_rate = 0.001
subscription_interval = 86400
event_rate = 0.0001
